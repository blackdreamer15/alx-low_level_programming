#include <stdio.h>
/**
 * main - main block
 * Description:  prints alphabet in lowercase, and
 * then in uppercase, followed by a new line.
 * Return: 0
 */
int main(void)
{
char letter = 'a';
while (letter <= 'z')
{
putchar(letter);
letter++;
}
letter = 'A';
while (c <= 'Z')
{
putchar(c);
letter++;
}
putchar('\n');
return (0);
}
